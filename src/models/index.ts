export enum Filters {
  category = 'category',
  sort = 'sort'
}

export enum Category {
  all = 'all',
  phones = 'phones',
  laptops = 'laptops',
  tablets = 'tablets'
};

export enum SortBy {
  name = 'name',
  price = 'price'
};
